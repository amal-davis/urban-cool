# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Raw DB dump — not tracked, regenerate locally as needed
urbancool.sql

# Stray duplicate of the real backend/ at the repo root (confirmed
# byte-identical source, plus its own venv/.env) — not tracked so it can't
# get committed a second time by accident. Left on disk; delete manually if
# it's not needed.
backend/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
